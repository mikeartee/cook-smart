service: cook-smart-api

provider:
  name: aws
  runtime: nodejs18.x
  region: us-east-1
  stage: ${opt:stage, 'dev'}
  memorySize: 256
  timeout: 30
  
  environment:
    DATABASE_URL: ${env:DATABASE_URL}
    JWT_SECRET: ${env:JWT_SECRET}
    JWT_EXPIRES_IN: ${env:JWT_EXPIRES_IN}
    NODE_ENV: ${env:NODE_ENV}
  
  iam:
    role:
      statements:
        - Effect: Allow
          Action:
            - rds:*
          Resource: "*"
        - Effect: Allow
          Action:
            - s3:*
          Resource: "*"

functions:
  register:
    handler: functions/auth/register.handler
    events:
      - http:
          path: auth/register
          method: post
          cors: true
  
  login:
    handler: functions/auth/login.handler
    events:
      - http:
          path: auth/login
          method: post
          cors: true
  
  me:
    handler: functions/auth/me.handler
    events:
      - http:
          path: auth/me
          method: get
          cors: true
  
  getProfile:
    handler: functions/users/getProfile.handler
    events:
      - http:
          path: users/profile
          method: get
          cors: true
  
  updateProfile:
    handler: functions/users/updateProfile.handler
    events:
      - http:
          path: users/profile
          method: put
          cors: true
  
  changePassword:
    handler: functions/users/changePassword.handler
    events:
      - http:
          path: users/password
          method: put
          cors: true
  
  exportData:
    handler: functions/users/exportData.handler
    events:
      - http:
          path: users/export
          method: get
          cors: true
  
  deleteAccount:
    handler: functions/users/deleteAccount.handler
    events:
      - http:
          path: users/account
          method: delete
          cors: true
  
  getUserIngredients:
    handler: functions/ingredients/getUserIngredients.handler
    events:
      - http:
          path: ingredients
          method: get
          cors: true
  
  addIngredient:
    handler: functions/ingredients/addIngredient.handler
    events:
      - http:
          path: ingredients
          method: post
          cors: true
  
  updateIngredient:
    handler: functions/ingredients/updateIngredient.handler
    events:
      - http:
          path: ingredients/{id}
          method: put
          cors: true
  
  deleteIngredient:
    handler: functions/ingredients/deleteIngredient.handler
    events:
      - http:
          path: ingredients/{id}
          method: delete
          cors: true
  
  searchIngredients:
    handler: functions/ingredients/searchIngredients.handler
    events:
      - http:
          path: ingredients/search
          method: get
          cors: true
  
  getUserRecipes:
    handler: functions/recipes/getUserRecipes.handler
    events:
      - http:
          path: recipes
          method: get
          cors: true
  
  saveRecipe:
    handler: functions/recipes/saveRecipe.handler
    events:
      - http:
          path: recipes
          method: post
          cors: true
  
  deleteRecipe:
    handler: functions/recipes/deleteRecipe.handler
    events:
      - http:
          path: recipes/{id}
          method: delete
          cors: true

plugins:
  - serverless-offline

custom:
  serverless-offline:
    httpPort: 3000
