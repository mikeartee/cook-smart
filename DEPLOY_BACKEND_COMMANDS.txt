# COPY AND PASTE THESE COMMANDS TO DEPLOY BACKEND

# Step 1: SSH to production server
ssh -i your-key.pem ubuntu@3.237.38.24

# Step 2: Navigate to backend directory
cd /home/ubuntu/cook-smart-backend

# Step 3: Pull latest code
git pull origin fresh-project-migration

# Step 4: Install any new dependencies
npm install

# Step 5: Build TypeScript
npm run build

# Step 6: Restart PM2
pm2 restart cook-smart-api

# Step 7: Check status
pm2 status

# Step 8: Check logs for errors
pm2 logs cook-smart-api --lines 50

# Step 9: Test endpoints (should return 401, not 404)
curl https://api.cooksmart.app/api/v1/subscriptions/me
curl https://api.cooksmart.app/api/v1/settings/privacy

# Done! The backend errors should stop now.
